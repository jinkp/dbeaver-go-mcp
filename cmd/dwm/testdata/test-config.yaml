project: TestProject
environments:
  - DEV
  - PROD
connections:
  - name: "{{.Project}} {{.Env}}"
    type: postgresql
    host: localhost
    port: 5432
    database: testdb
    folder: "{{.Env}}"
engines:
  - postgresql
